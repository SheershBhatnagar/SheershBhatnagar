import 'package:flutter/material.dart';

class AppColors {
  final Color? primaryColor = Colors.red[800];

  // BACKGROUND COLORS
  final Color backgroundColor = Color(0XFF161616);

  // TEXT COLORS
  final Color? fontDisableColor = Colors.grey[400];
}
